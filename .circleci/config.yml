version: 2.1

jobs:
  echo-global:
    docker:
      - image: cimg/base:stable
    steps:
      - run:
          name: Say hello
          command: echo "✅ This is the GLOBAL pipeline"

workflows:
  global-workflow:
    jobs:
      - echo-global
